import { SubscriptionPageLayout } from "@/components/landing/subscription-page-layout";

/** Subscription funnel preview under admin (same UI as public `/subscription`). */
export default function AdminSubscriptionPage() {
  return <SubscriptionPageLayout />;
}
