/** User-facing: add Stripe secret key (import from client and server). */
export const STRIPE_ENV_HELP =
  "Add STRIPE_SECRET_KEY or STRIPE_SECRET_KEY_TEST to your environment (e.g. .env.local or Vercel → Settings → Environment Variables), then restart the app.";
